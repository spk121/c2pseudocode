#include <math.h>

int main(void) {
    double x = 4.0;
    double y = 2.0;

    double sr = sqrt(x);
    double av = fabs(x);
    int ai = abs(-3);
    double pw = pow(x, y);
    double ex = exp(x);
    double ln = log(x);
    double l2 = log2(x);
    double l10 = log10(x);
    double cl = ceil(x);
    double fl = floor(x);
    double rd = round(x);
    double sn = sin(x);
    double cs = cos(x);
    double tn = tan(x);
    double as = asin(y);
    double ac = acos(y);
    double at = atan(y);
    double at2 = atan2(y, x);

    return 0;
}
